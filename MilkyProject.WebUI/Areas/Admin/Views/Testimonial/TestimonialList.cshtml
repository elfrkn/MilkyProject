@model List<MilkyProject.WebUI.DTOs.Testimonial.ResultTestimonialDto>
@{
    ViewData["Title"] = "TestimonialList";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}


<div class="row">
    <div class="col">
        <div class="card shadow">
            <div class="card-header border-0">
                <h3 class="mb-0">Referanslar</h3>
            </div>
            <div class="table-responsive">
                <table class="table align-items-center table-flush">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Resim Yolu</th>
                            <th scope="col">Ad Soyad</th>
                            <th scope="col">Açıklama</th>
                            <th scope="col">Sil</th>
                            <th scope="col">Güncelle</th>

                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                                <th scope="row">
                                    <div class="media align-items-center">
                                        <a href="#" class="avatar rounded-circle mr-3">
                                            <img alt="Image placeholder" src="@item.ImageUrl">
                                        </a>
                                    </div>
                                </th>
                                <td>
                                    @item.Name
                                </td>
                                <td>
                                    @item.Description.Substring(0,25)...
                                </td>

                                <td><a href="/Admin/Testimonial/DeleteTestimonial/@item.TestimonialID" class="btn btn-warning">Sil</a></td>
                                <td><a href="/Admin/Testimonial/UpdateTestimonial/@item.TestimonialID" class="btn btn-info">Güncelle</a></td>
                            </tr>
                        }
                    </tbody>
                </table>

            </div>
        </div>
        <br />
        <td><a href="/Admin/Testimonial/CreateTestimonial/" class="btn btn-primary">Yeni Referans Girişi</a></td>
    </div>
</div>